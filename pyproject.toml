[project]
name = "mcp-host"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "groq>=1.5.0",
    "mcp<2",
    "python-dotenv>=1.2.2",
    "python-telegram-bot>=22.8",
]
