GROQ_API_KEY=
TELEGRAM_BOT_TOKEN=

# Google Calendar MCP server (optional — only needed if using the calendar server)
# CREDENTIALS_PATH defaults to ./credentials.json if not set
# CREDENTIALS_PATH=./credentials.json
